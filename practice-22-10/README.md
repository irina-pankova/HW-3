# Практика к семинару от 22.10

Ниже приведён список bash-скриптов и их назначение:

| Файл | Назначение |
|------|-------------|
| **task1.sh** | Подсчёт количества вхождений слова в файле (`grep` + `wc -l`) |
| **task2.sh** | Сравнение двух чисел и вывод результата ("больше / меньше / равны") |
| **task3.sh** | Генератор паролей заданной длины (использует `/dev/urandom` и `sha256sum`) |
| **task3v2.sh** | Генератор пароля с хэшом даты и времени до наносекунд |
| **task4.sh** | Проверка доступности сервера (`ping`) и вывод статуса |
| **task5.sh** | Преобразование имён файлов в директории `for_task_5` в нижний регистр |
| **task6.sh** | Проверка, когда последний раз изменялся файл — более или менее минуты назад |
| **task7.sh** | Пример отправки сообщения через Telegram API *(демо, токен отозван перед публикацией)* |
| **task8.sh** | Телеграм-бот, реагирующий на команду `Дата` и возвращающий текущую дату *(токен отозван перед публикацией)* |
| **task9.sh** | Создание логов, сортировка по дате и вывод 5 самых старых (`ls -tr | head -5`) |
| **task10.sh** | Запуск нескольких процессов параллельно и ожидание завершения всех (`wait`) |
| **text.txt** | Вспомогательный текстовый файл для заданий |

---

📁 **Структура папок:**
- `for_task_5` — тестовые файлы для проверки переименования.
- `for_task_8` — лог-файлы для сортировки и анализа.

---

🔒 *Все токены Telegram были отозваны до загрузки в публичный репозиторий.*

